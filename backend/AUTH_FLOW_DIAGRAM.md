# 🔐 Authentication Flow Diagrams

Visual representation of all authentication flows in the system.

---

## 1️⃣ User Registration Flow

```
┌─────────────┐
│   Client    │
│  (Frontend) │
└──────┬──────┘
       │
       │ POST /api/auth/register
       │ { name, email, phone, password }
       ▼
┌─────────────────────────────────────┐
│         Backend Server              │
│  ┌───────────────────────────────┐  │
│  │  1. Validate input data       │  │
│  │  2. Check email/phone unique  │  │
│  │  3. Hash password (bcrypt)    │  │
│  │  4. Create user in DB         │  │
│  │  5. Generate JWT token        │  │
│  │  6. Send welcome email        │  │
│  └───────────────────────────────┘  │
└──────┬──────────────────────────────┘
       │
       │ Response: { success, data: { user, token } }
       ▼
┌─────────────┐
│   Client    │
│ Store token │
│ Redirect to │
│  Dashboard  │
└─────────────┘
```

---

## 2️⃣ User Login Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ POST /api/auth/login
       │ { email, password }
       ▼
┌─────────────────────────────────────┐
│         Backend Server              │
│  ┌───────────────────────────────┐  │
│  │  1. Find user by email        │  │
│  │     ├─ Not found?             │  │
│  │     │  → "Invalid email"      │  │
│  │     └─ Found ✓                │  │
│  │                               │  │
│  │  2. Compare password          │  │
│  │     ├─ Wrong?                 │  │
│  │     │  → "Incorrect password" │  │
│  │     └─ Correct ✓              │  │
│  │                               │  │
│  │  3. Generate JWT token        │  │
│  │  4. Return user + token       │  │
│  └───────────────────────────────┘  │
└──────┬──────────────────────────────┘
       │
       │ Response: { success, data: { user, token } }
       ▼
┌─────────────┐
│   Client    │
│ Store token │
│ Redirect to │
│  Dashboard  │
└─────────────┘
```

---

## 3️⃣ Forgot Password Flow

```
┌─────────────┐
│   Client    │
│ Forgot Pass │
│    Page     │
└──────┬──────┘
       │
       │ POST /api/auth/forgot-password
       │ { email }
       ▼
┌─────────────────────────────────────────────┐
│            Backend Server                   │
│  ┌───────────────────────────────────────┐  │
│  │  1. Find user by email                │  │
│  │     └─ Not found? → Error             │  │
│  │                                       │  │
│  │  2. Generate secure random token     │  │
│  │     (32 bytes crypto)                 │  │
│  │                                       │  │
│  │  3. Save to password_reset_tokens    │  │
│  │     - token                           │  │
│  │     - userId                          │  │
│  │     - expiresAt (now + 1 hour)       │  │
│  │     - used: false                     │  │
│  │                                       │  │
│  │  4. Send email with reset link       │  │
│  │     Link: /reset-password/:token     │  │
│  └───────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │
       │ Response: { success, message }
       ▼
┌─────────────┐         ┌──────────────┐
│   Client    │         │  User Email  │
│ Show success│         │              │
│   message   │         │ ┌──────────┐ │
└─────────────┘         │ │ Reset    │ │
                        │ │ Password │ │
                        │ │  Link    │ │
                        │ └────┬─────┘ │
                        └──────┼───────┘
                               │
                               ▼
                        ┌─────────────┐
                        │ Reset Pass  │
                        │    Page     │
                        └─────────────┘
```

---

## 4️⃣ Reset Password Flow

```
┌─────────────┐
│   Client    │
│ Reset Pass  │
│    Page     │
└──────┬──────┘
       │
       │ POST /api/auth/reset-password/:token
       │ { password }
       ▼
┌─────────────────────────────────────────────┐
│            Backend Server                   │
│  ┌───────────────────────────────────────┐  │
│  │  1. Find token in DB                  │  │
│  │     WHERE:                            │  │
│  │     - token = :token                  │  │
│  │     - used = false                    │  │
│  │     - expiresAt > now                 │  │
│  │                                       │  │
│  │     Not found? → "Invalid/expired"   │  │
│  │                                       │  │
│  │  2. Find user by userId               │  │
│  │                                       │  │
│  │  3. Hash new password (bcrypt)        │  │
│  │                                       │  │
│  │  4. Update user.password              │  │
│  │                                       │  │
│  │  5. Mark token as used                │  │
│  │     token.used = true                 │  │
│  └───────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │
       │ Response: { success, message }
       ▼
┌─────────────┐
│   Client    │
│ Show success│
│ Redirect to │
│    Login    │
└─────────────┘
```

---

## 5️⃣ Protected Route Access Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ GET /api/user/profile
       │ Header: Authorization: Bearer <token>
       ▼
┌─────────────────────────────────────────────┐
│            Backend Server                   │
│  ┌───────────────────────────────────────┐  │
│  │  Middleware: protect                  │  │
│  │                                       │  │
│  │  1. Check Authorization header        │  │
│  │     No token? → 401 "No token"       │  │
│  │                                       │  │
│  │  2. Extract token from Bearer        │  │
│  │                                       │  │
│  │  3. Verify JWT signature              │  │
│  │     Invalid? → 401 "Token failed"    │  │
│  │                                       │  │
│  │  4. Decode token → get userId         │  │
│  │                                       │  │
│  │  5. Find user in DB                   │  │
│  │     Not found? → 401 "User not found"│  │
│  │                                       │  │
│  │  6. Attach user to req.user           │  │
│  │                                       │  │
│  │  7. Call next() → Controller          │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  Controller: getUserProfile           │  │
│  │                                       │  │
│  │  1. Get user from req.user            │  │
│  │  2. Return user data (no password)    │  │
│  └───────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │
       │ Response: { success, data: { user } }
       ▼
┌─────────────┐
│   Client    │
│ Display     │
│  Profile    │
└─────────────┘
```

---

## 6️⃣ Admin Route Access Flow

```
┌─────────────┐
│   Client    │
│ (Admin User)│
└──────┬──────┘
       │
       │ GET /api/admin/users
       │ Header: Authorization: Bearer <token>
       ▼
┌─────────────────────────────────────────────┐
│            Backend Server                   │
│  ┌───────────────────────────────────────┐  │
│  │  Middleware: protect                  │  │
│  │  (Same as above)                      │  │
│  │  → Verify token                       │  │
│  │  → Attach req.user                    │  │
│  └───────────────────────────────────────┘  │
│                    ▼                        │
│  ┌───────────────────────────────────────┐  │
│  │  Middleware: admin                    │  │
│  │                                       │  │
│  │  1. Check req.user.role               │  │
│  │     ├─ role !== 'admin'?             │  │
│  │     │  → 403 "Not authorized"        │  │
│  │     └─ role === 'admin' ✓            │  │
│  │                                       │  │
│  │  2. Call next() → Controller          │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  Controller: adminFunction            │  │
│  │  (Admin-only logic)                   │  │
│  └───────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │
       │ Response: { success, data }
       ▼
┌─────────────┐
│   Client    │
│   Admin     │
│  Dashboard  │
└─────────────┘
```

---

## 7️⃣ Profile Image Upload Flow

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       │ POST /api/user/profile/image
       │ Header: Authorization: Bearer <token>
       │ Body: FormData { profileImage: [FILE] }
       ▼
┌─────────────────────────────────────────────┐
│            Backend Server                   │
│  ┌───────────────────────────────────────┐  │
│  │  Middleware: protect                  │  │
│  │  → Verify JWT token                   │  │
│  └───────────────────────────────────────┘  │
│                    ▼                        │
│  ┌───────────────────────────────────────┐  │
│  │  Middleware: multer upload            │  │
│  │                                       │  │
│  │  1. Validate file type                │  │
│  │     (jpeg, jpg, png, gif, webp)       │  │
│  │                                       │  │
│  │  2. Check file size (< 5MB)           │  │
│  │                                       │  │
│  │  3. Generate unique filename          │  │
│  │     profile-{userId}-{timestamp}.jpg  │  │
│  │                                       │  │
│  │  4. Save to uploads/profiles/         │  │
│  │                                       │  │
│  │  5. Attach req.file                   │  │
│  └───────────────────────────────────────┘  │
│                    ▼                        │
│  ┌───────────────────────────────────────┐  │
│  │  Controller: uploadProfileImage       │  │
│  │                                       │  │
│  │  1. Get user from req.user            │  │
│  │                                       │  │
│  │  2. Delete old image if exists        │  │
│  │                                       │  │
│  │  3. Update user.profileImage          │  │
│  │     = /uploads/profiles/filename.jpg  │  │
│  │                                       │  │
│  │  4. Save to database                  │  │
│  └───────────────────────────────────────┘  │
└──────┬──────────────────────────────────────┘
       │
       │ Response: { success, data: { profileImage } }
       ▼
┌─────────────┐
│   Client    │
│ Display new │
│   image     │
└─────────────┘
```

---

## 8️⃣ Complete User Journey

```
┌──────────────────────────────────────────────────────────────┐
│                     USER JOURNEY                             │
└──────────────────────────────────────────────────────────────┘

1. REGISTRATION
   ┌─────────┐
   │ Sign Up │ → Email sent → Welcome bonus (500 coins)
   └────┬────┘
        │
        ▼
2. LOGIN
   ┌─────────┐
   │  Login  │ → JWT token → Store in localStorage
   └────┬────┘
        │
        ▼
3. DASHBOARD (Protected)
   ┌──────────────┐
   │   Dashboard  │ → View profile, orders, etc.
   │              │ → Upload profile image
   │   [Profile]  │ → Update information
   └──────┬───────┘
          │
          ├─→ Forgot Password?
          │   ┌──────────────┐
          │   │ Reset Email  │ → Click link → New password
          │   └──────────────┘
          │
          └─→ Admin User?
              ┌──────────────┐
              │ Admin Panel  │ → Manage users, products
              └──────────────┘
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                      │
└─────────────────────────────────────────────────────────┘

Layer 1: HTTPS (Production)
         ↓
Layer 2: CORS & Helmet Headers
         ↓
Layer 3: JWT Token Verification
         ↓
Layer 4: Role-Based Access Control
         ↓
Layer 5: Input Validation
         ↓
Layer 6: Password Hashing (bcrypt)
         ↓
Layer 7: SQL Injection Protection (Sequelize ORM)
         ↓
Layer 8: File Upload Validation
         ↓
Layer 9: Rate Limiting (Future)
```

---

## 📊 Database Relationships

```
┌─────────────────────────────────────────────────────────┐
│                  DATABASE SCHEMA                        │
└─────────────────────────────────────────────────────────┘

┌──────────────┐
│    Users     │
│──────────────│
│ id (PK)      │───┐
│ name         │   │
│ email        │   │
│ password     │   │
│ role         │   │
│ profileImage │   │
│ ...          │   │
└──────────────┘   │
                   │ 1:N
                   │
                   ▼
        ┌──────────────────────┐
        │ PasswordResetTokens  │
        │──────────────────────│
        │ id (PK)              │
        │ userId (FK) ─────────┘
        │ token (unique)       │
        │ expiresAt            │
        │ used                 │
        └──────────────────────┘

Token Lifecycle:
1. Created → used: false, expiresAt: now + 1h
2. Used → used: true (cannot be reused)
3. Expired → expiresAt < now (invalid)
```

---

## 🔄 Token Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│              PASSWORD RESET TOKEN STATES                │
└─────────────────────────────────────────────────────────┘

[CREATED]
   │
   │ User requests password reset
   │ Token generated & saved
   │ Email sent
   │
   ▼
[ACTIVE] ─────────────────┐
   │                      │
   │ User clicks link     │ 1 hour passes
   │ Enters new password  │
   │                      │
   ▼                      ▼
[USED]                [EXPIRED]
   │                      │
   └──────────┬───────────┘
              │
              ▼
         [INVALID]
    (Cannot be used again)
```

---

## 📧 Email Flow

```
┌─────────────────────────────────────────────────────────┐
│                    EMAIL SYSTEM                         │
└─────────────────────────────────────────────────────────┘

Backend Server
      │
      ├─→ Registration
      │   └─→ sendWelcomeEmail()
      │       ├─ Subject: "Welcome to DentalShop!"
      │       ├─ Content: Welcome message + bonus info
      │       └─ Non-blocking (async)
      │
      └─→ Forgot Password
          └─→ sendPasswordResetEmail()
              ├─ Subject: "Password Reset Request"
              ├─ Content: Reset link + expiry info
              ├─ Link: /reset-password/:token
              └─ Blocking (must succeed)

Email Service (Nodemailer)
      │
      └─→ Gmail SMTP
          ├─ Host: smtp.gmail.com
          ├─ Port: 587
          ├─ Auth: App Password
          └─ TLS: Enabled
```

---

## 🎯 Request/Response Flow

```
┌─────────────────────────────────────────────────────────┐
│              TYPICAL API REQUEST FLOW                   │
└─────────────────────────────────────────────────────────┘

Client Request
      │
      ▼
┌──────────────┐
│   Express    │
│  Middleware  │
└──────┬───────┘
       │
       ├─→ helmet() ────────→ Security headers
       ├─→ cors() ─────────→ CORS validation
       ├─→ express.json() ─→ Parse JSON body
       ├─→ cookieParser() ─→ Parse cookies
       │
       ▼
┌──────────────┐
│    Route     │
│   Handler    │
└──────┬───────┘
       │
       ├─→ protect ────────→ JWT verification
       ├─→ admin ──────────→ Role check (if needed)
       ├─→ upload ─────────→ File upload (if needed)
       │
       ▼
┌──────────────┐
│  Controller  │
│   Function   │
└──────┬───────┘
       │
       ├─→ Validate input
       ├─→ Database query (Sequelize)
       ├─→ Business logic
       ├─→ Format response
       │
       ▼
┌──────────────┐
│   Response   │
│   to Client  │
└──────────────┘
```

---

**Visual guide to understanding the authentication system architecture**
